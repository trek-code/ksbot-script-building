# KSBot Test Notes

Record every physical KSBot run here, including failed startup attempts.

## Standards

- One file per run or revision cycle
- Always include expected behavior before the test begins
- Capture the first failure point, not just the final result
- Note whether the issue came from code, docs, transfer setup, or RSPS behavior

Use `templates/checklists/TestRunTemplate.md` as the base format.
