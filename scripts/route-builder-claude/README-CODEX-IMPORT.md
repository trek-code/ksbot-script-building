# Claude Import: Route Builder

This folder is a copied import of Claude's `route-builder` KSBot script from:

`D:\Claude Code\OLDER GPT Agent code framework\RSPS\KSBOT Script building\scripts\route-builder`

It has been copied into the active Codex workspace for review and possible reuse.

Keep this folder separate from live script folders until we decide what parts to adopt.
